{
  "name": "filebrowser",
  "version": "0.3.1",
  "description": "FILES explorer: browse/read/write/create/remove inside the session cwd, with a built-in color editor (syntax-highlighted code, colored markdown source). Ships its own statusbar UI.",
  "provides": [
    "rpc",
    "panel"
  ],
  "requires": [],
  "ui": {
    "module": "ui.js",
    "element": "xu-filebrowser",
    "mount": "statusbar"
  },
  "settings": [
    {
      "key": "max_bytes",
      "type": "integer",
      "default": 1048576,
      "label": "Refuse to read/write files larger than this (bytes)."
    }
  ]
}
