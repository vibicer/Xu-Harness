{
  "name": "mnemosyne",
  "version": "0.1.0",
  "description": "Mnemosyne long-term memory: recall digest injected before each LLM call (before_llm hook) + remember/recall tools. Requires `pip install mnemosyne-memory` in the brain env; fail-soft without it.",
  "provides": [
    "tool",
    "hook"
  ],
  "requires": [],
  "settings": [
    {
      "key": "inject",
      "type": "boolean",
      "default": true,
      "label": "Inject a recall digest into the system prompt on every turn."
    },
    {
      "key": "top_k",
      "type": "integer",
      "default": 5,
      "label": "Memories in the injected digest."
    },
    {
      "key": "max_chars",
      "type": "integer",
      "default": 2000,
      "label": "Hard cap (characters) on the injected digest."
    }
  ]
}